<configuration>
    <!-- Jawisp auto-config - users don't need to know -->
    <logger name="io.netty" level="WARN"/>
    <logger name="io.netty.channel" level="WARN"/>
    <logger name="io.netty.util" level="WARN"/>
    <logger name="io.netty.handler" level="WARN"/>
    <logger name="io.jawisp" level="INFO"/>
    
    <!-- Minimal console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <logger name="io.netty.util.internal.CleanerJava9" level="WARN"/>
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>
</configuration>
